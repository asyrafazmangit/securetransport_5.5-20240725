Step Install Axway in Red Hat Enterprise 9 & Rocky Linux 9
===========================================================================================

# 1) Install the runtime/dependencies 
-glibc
-glibc-langpack-en
-libxml2
-libxslt
-libaio
-zlib

# Update the system
-sudo dnf update -y

# Install the packages (dependencies/runtime)
-sudo dnf install -y glibc glibc-langpack-en libxml2 libxslt libaio zlib

#Verify Installation
-rpm -q glibc glibc-langpack-en libxml2 libxslt libaio zlib

# 2) Error: Unable to find a match: ncurses-compat-libs

# Enable Additional Repositories
#EPEL (Extra Packages for Enterprise Linux) repository
-sudo dnf install -y epel-release
-sudo dnf update -y

# Search for ncurses-compat-libs
-sudo dnf search ncurses-compat-libs

# Install Compatibility Libraries
-sudo dnf install -y ncurses ncurses-libs ncurses-devel

# 3)  Install Perl Data Dumper/Perl Getopt Long/Ncurses Compat Libs
-sudo dnf install -y perl perl-Data-Dumper perl-Getopt-Long ncurses-compat-libs

# Verify the installation
-perl --version






